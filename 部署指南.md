# 🚀 ESG 平台部署指南

本指南提供多种方式将您的本地应用分享给其他人访问。

---

## 📋 目录

1. [快速方案：内网穿透（推荐新手）](#1-快速方案内网穿透推荐新手)
   - [方案 A：使用 ngrok（最简单）](#方案-a使用-ngrok最简单)
   - [方案 B：使用 Cloudflare Tunnel（免费）](#方案-b使用-cloudflare-tunnel免费)
   - [方案 C：使用 localtunnel（无需注册）](#方案-c使用-localtunnel无需注册)

2. [专业方案：云平台部署（推荐生产环境）](#2-专业方案云平台部署推荐生产环境)
   - [方案 A：Railway（最简单，推荐）](#方案-arailway最简单推荐)
   - [方案 B：Render（免费额度）](#方案-brender免费额度)
   - [方案 C：DigitalOcean App Platform](#方案-cdigitalocean-app-platform)

3. [VPS 部署（需要自有服务器）](#3-vps-部署需要自有服务器)

4. [常见问题](#常见问题)

---

## 1. 快速方案：内网穿透（推荐新手）

### 方案 A：使用 ngrok（最简单）

**优点：** 设置最快，5分钟即可完成  
**缺点：** 免费版每次重启会改变URL

#### 步骤：

1. **注册 ngrok 账号**
   - 访问：https://ngrok.com/
   - 注册并获取免费 authtoken

2. **安装 ngrok**
   ```bash
   # Windows: 下载 https://ngrok.com/download
   # 或使用 Chocolatey
   choco install ngrok
   
   # Mac
   brew install ngrok/ngrok/ngrok
   ```

3. **配置 authtoken**
   ```bash
   ngrok config add-authtoken YOUR_AUTH_TOKEN
   ```

4. **启动您的应用**
   ```bash
   npm start
   ```

5. **在另一个终端启动 ngrok**
   ```bash
   ngrok http 3000
   ```

6. **分享 URL**
   - ngrok 会显示类似：`https://xxxx-xx-xx-xx-xx.ngrok-free.app`
   - 将这个 URL 分享给其他人即可访问

#### 固定域名（付费版）

如果需要固定 URL，可以升级到付费版，或使用下面的方案。

---

### 方案 B：使用 Cloudflare Tunnel（免费）

**优点：** 完全免费，可以设置固定域名  
**缺点：** 设置稍复杂

#### 步骤：

1. **安装 cloudflared**
   ```bash
   # Windows: 下载 https://github.com/cloudflare/cloudflared/releases
   # 或使用 Chocolatey
   choco install cloudflared
   ```

2. **登录 Cloudflare**
   ```bash
   cloudflared tunnel login
   ```

3. **创建隧道**
   ```bash
   cloudflared tunnel create esg-platform
   ```

4. **启动隧道**
   ```bash
   cloudflared tunnel --url http://localhost:3000
   ```

5. **分享 URL**
   - 会显示类似：`https://xxxx-xxxx.trycloudflare.com`
   - 将这个 URL 分享给其他人

---

### 方案 C：使用 localtunnel（无需注册）

**优点：** 无需注册，直接使用  
**缺点：** URL 每次都会变化

#### 步骤：

1. **全局安装 localtunnel**
   ```bash
   npm install -g localtunnel
   ```

2. **启动您的应用**
   ```bash
   npm start
   ```

3. **在另一个终端启动隧道**
   ```bash
   lt --port 3000
   ```

4. **分享 URL**
   - 会显示类似：`https://xxxx.loca.lt`
   - 将这个 URL 分享给其他人

---

## 2. 专业方案：云平台部署（推荐生产环境）

### 方案 A：Railway（最简单，推荐）

**优点：** 部署最简单，有免费额度，自动 HTTPS  
**缺点：** 免费额度有限

#### 步骤：

1. **准备项目**

   创建 `Procfile`（如果不存在）：
   ```
   web: node server.js
   ```

   确保 `package.json` 中有 `start` 脚本（已有）

2. **部署到 Railway**

   - 访问：https://railway.app/
   - 使用 GitHub 账号登录
   - 点击 "New Project" → "Deploy from GitHub repo"
   - 选择您的项目仓库
   - Railway 会自动检测 Node.js 项目并部署

3. **配置环境变量**

   在 Railway 项目设置中添加：
   ```
   PORT=3000
   GEMINI_API_KEY=your_gemini_api_key_here
   NODE_ENV=production
   ```

4. **获取访问 URL**

   - Railway 会自动生成一个 URL，类似：`https://your-project.up.railway.app`
   - 这个 URL 是固定的，可以分享给其他人

#### 免费额度：
- $5/月免费额度
- 500小时运行时间
- 足够小型项目使用

---

### 方案 B：Render（免费额度）

**优点：** 免费版可用，设置简单  
**缺点：** 免费版有睡眠机制（15分钟无请求会休眠）

#### 步骤：

1. **准备项目**

   创建 `render.yaml`（可选）：
   ```yaml
   services:
     - type: web
       name: esg-platform
       env: node
       buildCommand: npm install
       startCommand: npm start
       envVars:
         - key: PORT
           value: 3000
         - key: NODE_ENV
           value: production
   ```

2. **部署到 Render**

   - 访问：https://render.com/
   - 使用 GitHub 账号登录
   - 点击 "New" → "Web Service"
   - 连接您的 GitHub 仓库
   - Render 会自动检测并配置

3. **配置环境变量**

   在 Render 控制台的 Environment 标签中添加：
   ```
   GEMINI_API_KEY=your_gemini_api_key_here
   PORT=3000
   ```

4. **获取访问 URL**

   - Render 会生成：`https://your-project.onrender.com`
   - 免费版首次访问可能需要等待几秒（唤醒服务）

---

### 方案 C：DigitalOcean App Platform

**优点：** 性能好，有免费额度  
**缺点：** 需要信用卡验证

#### 步骤：

1. **访问 DigitalOcean**
   - https://www.digitalocean.com/
   - 注册账号（需要信用卡验证）

2. **创建 App**
   - 点击 "Create" → "Apps"
   - 连接 GitHub 仓库
   - 选择 "Web Service"

3. **配置**
   - Build Command: `npm install`
   - Run Command: `npm start`
   - 添加环境变量

4. **部署**
   - 点击 "Create Resources"
   - 等待部署完成

---

## 3. VPS 部署（需要自有服务器）

如果您有自己的 VPS（如 AWS EC2、DigitalOcean Droplet、阿里云等），可以按以下步骤部署：

### 步骤：

1. **连接到服务器**
   ```bash
   ssh user@your-server-ip
   ```

2. **安装 Node.js**
   ```bash
   # 使用 nvm（推荐）
   curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
   nvm install 18
   nvm use 18
   ```

3. **克隆项目**
   ```bash
   git clone your-repo-url
   cd your-project
   npm install
   ```

4. **配置环境变量**
   ```bash
   cp .env.example .env
   nano .env  # 编辑并填入配置
   ```

5. **使用 PM2 管理进程**
   ```bash
   npm install -g pm2
   pm2 start server.js --name esg-platform
   pm2 save
   pm2 startup  # 设置开机自启
   ```

6. **配置 Nginx 反向代理**
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

7. **配置 SSL（使用 Let's Encrypt）**
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com
   ```

---

## 常见问题

### Q1: 哪种方案最适合我？

- **快速测试/演示**：使用 ngrok 或 localtunnel（5分钟）
- **短期分享**：使用 Railway 或 Render（免费版）
- **长期使用**：使用 Railway 付费版或 VPS

### Q2: 环境变量如何配置？

创建 `.env` 文件（不要提交到 Git）：
```
PORT=3000
GEMINI_API_KEY=your_api_key_here
NODE_ENV=production
```

### Q3: 免费版有限制吗？

- **ngrok 免费版**：每次重启 URL 会变化
- **Render 免费版**：15分钟无请求会休眠，首次访问需要等待
- **Railway 免费版**：$5/月额度，超出需要付费

### Q4: 如何确保安全性？

1. **不要将 `.env` 文件提交到 Git**
2. **使用 HTTPS**（所有云平台都自动提供）
3. **设置 CORS 限制**（如果需要）
4. **添加速率限制**（防止滥用）

### Q5: 部署后无法访问？

检查清单：
- [ ] 服务器是否正在运行？
- [ ] 端口是否正确？
- [ ] 防火墙是否开放端口？
- [ ] 环境变量是否配置正确？
- [ ] 查看服务器日志：`pm2 logs` 或云平台日志

---

## 🎯 推荐流程

### 快速演示（今天就要用）
1. 安装 ngrok：`choco install ngrok` 或下载
2. 启动应用：`npm start`
3. 启动隧道：`ngrok http 3000`
4. 分享 URL

### 正式部署（给团队长期使用）
1. 将代码推送到 GitHub
2. 在 Railway 创建项目并连接 GitHub
3. 配置环境变量
4. 等待部署完成
5. 分享固定 URL

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. 服务器日志
2. 浏览器控制台（F12）
3. 网络连接
4. 环境变量配置

祝部署顺利！🎉

